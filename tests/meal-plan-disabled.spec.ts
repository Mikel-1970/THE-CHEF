import {test,expect} from '@playwright/test';
test.beforeEach(async({page})=>{await page.addInitScript(()=>{for(const k of ['chef:home-greeted:v2','chef:auth:session:v1','chef:entry-tutorial:seen-session:v1','chef:tutorial:invite-dismissed-session:v2'])sessionStorage.setItem(k,'1')});});
for(const path of ['plan-comidas','plan-semanal'])test(`retired ${path} link returns home without loading or requesting a plan`,async({page})=>{let calls=0;await page.route('**/recipes/**',r=>{calls++;return r.abort()});await page.goto('./#/'+path);await expect(page.locator('.reference-desire-card')).toBeVisible();await expect(page).toHaveURL(/#\/$/);await expect(page.getByText('Mi plan de comidas',{exact:true})).toHaveCount(0);await expect(page.locator('.reference-secondary-card')).toHaveCount(1);await page.getByRole('button',{name:'Abrir menú',exact:true}).click();await expect(page.locator('.chef-menu-grid')).not.toContainText('Mi plan');expect(calls).toBe(0);});
