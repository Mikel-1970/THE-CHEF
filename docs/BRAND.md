# Chef Voldi identity

Approved source supplied by the owner: `Imagen de Codex 25 sept 2026, 20_01_45.png` in CHEF VOLDI. The original board is retained byte-for-byte as public/brand/chef-voldi-board.png (SHA-256 CDB6F44BE0FD95F6DBF284730259CB01DE48B4765BB23EBE946572C2C5D16894).

BrandMark displays windows into that source. PDF and social exports draw the same regions without changing their aspect ratio. No generative reconstruction or vector-master claim. The favicon is a self-contained SVG viewport over the original raster artwork. Native home-screen installation remains a device acceptance check.

The brand name is Chef Voldi by VollDium. Existing personal avatar selections, persistence keys, repository names and routes remain compatible. Voldi is the default for new profiles. Unknown legacy avatar values retain the safe human-chef fallback.

The palette is cream, chocolate, brown and caramel from the supplied board. Darker brown is used for readable action labels. Recipe PDF chapter boundaries are retained. No paid image generation is used.
